from setuptools import find_packages, setup

packages = ['gridtools',
            'gridtools.toolbox',
            'gridtools.prepro']

setup(
    name="gridtools",
    version="0.0.1",
    author="",
    author_email="",
    description="",
    packages=find_packages(),
)
